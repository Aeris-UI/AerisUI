# Workspace tools

This directory contains deterministic repository tooling for documentation generation, package
validation, Angular initialization schematics, release packaging, and local development. Each tool
is invoked through a named root `package.json` script so CI and contributor workflows use the same
commands.
